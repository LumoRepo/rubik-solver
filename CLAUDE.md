# Ground Rules

1. Save all conversations as they happen to prevent losing them when something crashes.
2. Do not ask for confirmation to list, view, or edit files in the project folder, to change folders, configure environment variables, to use gradle, to build and deploy, to use adb, to use git.
3. I am working in Antigravity IDE on Windows.